@extends('layouts.app')
@section('title',$event->name.' 成績管理')
@section('content')
<div class="mx-auto max-w-6xl space-y-6 px-4 py-8 sm:px-6"><div><a href="{{ route('organizer.events.show',$event) }}" class="text-sm text-indigo-600">← 返回賽事工作台</a><h1 class="mt-2 text-2xl font-bold">成績確認與發布</h1><p class="mt-1 text-sm text-gray-500">只有選手送出且經主辦方確認的成績可以發布，發布後才能作為正式 Achievement 依據。</p></div>@if(session('success'))<div class="rounded-xl bg-green-50 p-4 text-sm text-green-700">{{ session('success') }}</div>@endif @if($errors->any())<div class="rounded-xl bg-red-50 p-4 text-sm text-red-700">{{ $errors->first() }}</div>@endif
<form id="verify-form" method="POST" action="{{ route('organizer.events.results.verify',$event) }}" class="hidden">@csrf</form><div class="overflow-hidden rounded-2xl border bg-white"><table class="min-w-full text-sm"><thead class="bg-gray-50 text-left text-xs text-gray-500"><tr><th class="p-3"><input type="checkbox" onclick="document.querySelectorAll('.result-check').forEach(el=>el.checked=this.checked)"></th><th class="p-3">選手</th><th class="p-3">組別</th><th class="p-3">總分</th><th class="p-3">選手送出</th><th class="p-3">主辦確認</th><th class="p-3">已發布</th></tr></thead><tbody class="divide-y">@foreach($registrations as $registration)<tr><td class="p-3">@if($registration->score_submitted_at && !$registration->score_verified_at)<input form="verify-form" class="result-check rounded" type="checkbox" name="registration_ids[]" value="{{ $registration->id }}">@endif</td><td class="p-3 font-medium">{{ $registration->name }}</td><td class="p-3">{{ $registration->event_group?->name }}</td><td class="p-3 font-semibold">{{ $registration->calculated_total }}</td><td class="p-3">{{ $registration->score_submitted_at?'是':'否' }}</td><td class="p-3">{{ $registration->score_verified_at?'已確認':'待確認' }}</td><td class="p-3">{{ $registration->result_published_at?'已發布':'—' }}</td></tr>@endforeach</tbody></table><div class="flex flex-wrap justify-end gap-2 border-t p-4"><button form="verify-form" class="rounded-xl bg-indigo-600 px-4 py-2 text-sm text-white">確認選取成績</button><form method="POST" action="{{ route('organizer.events.results.publish',$event) }}" onsubmit="return confirm('確定發布所有已確認成績？')">@csrf<button class="rounded-xl bg-green-600 px-4 py-2 text-sm text-white">發布正式成績</button></form></div></div></div>
@endsection
