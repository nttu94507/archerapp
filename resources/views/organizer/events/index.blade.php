@extends('layouts.app')
@section('title', '主辦方中心')
@section('content')
<div class="mx-auto max-w-6xl space-y-6 px-4 py-8 sm:px-6"><div class="flex items-start justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-widest text-indigo-600">Organizer</p><h1 class="mt-1 text-2xl font-bold">我的賽事</h1><p class="mt-1 text-sm text-gray-500">管理賽事、報名、工作人員、成績與 Badge。</p></div>@if(auth()->user()->canCreateEvents())<a href="{{ route('organizer.events.create') }}" class="rounded-xl bg-indigo-600 px-5 py-2.5 text-sm font-medium text-white">建立賽事</a>@else<a href="{{ route('organizer.qualification.show') }}" class="rounded-xl bg-indigo-600 px-5 py-2.5 text-sm font-medium text-white">申請主辦方資格</a>@endif</div>
@if(!auth()->user()->canCreateEvents())<div class="rounded-2xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-800"><p class="font-semibold">目前不能建立新賽事</p><p class="mt-1">資格狀態：{{ ['draft'=>'草稿','pending'=>'審核中','changes_requested'=>'待補件','rejected'=>'未通過','suspended'=>'已停權','legacy_review'=>'既有主辦方待審'][$organizerProfile?->status] ?? '尚未申請' }}。你仍可管理被指派的既有賽事。</p></div>@endif
<div class="grid gap-4 md:grid-cols-2">@forelse($events as $event)<a href="{{ route('organizer.events.show',$event) }}" class="rounded-2xl border bg-white p-5 shadow-sm hover:border-indigo-300"><div class="flex justify-between gap-3"><div><h2 class="font-semibold">{{ $event->name }}</h2><p class="mt-1 text-sm text-gray-500">{{ $event->start_date->format('Y-m-d') }}～{{ $event->end_date->format('Y-m-d') }}</p></div><span class="h-fit rounded-full px-2 py-1 text-xs {{ $event->isPublished() ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-600' }}">{{ ['draft'=>'草稿','pending'=>'審核中','approved'=>'已發布','rejected'=>'已退回','archived'=>'已封存'][$event->status] ?? $event->status }}</span></div><div class="mt-4 flex gap-4 border-t pt-3 text-sm text-gray-600"><span>{{ $event->groups_count }} 組</span><span>{{ $event->registrations_count }} 報名</span><span>{{ $event->badges_count }} Badge</span></div></a>@empty<p class="text-sm text-gray-500">目前沒有負責的賽事。</p>@endforelse</div>{{ $events->links() }}</div>
@endsection
